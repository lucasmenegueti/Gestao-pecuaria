# Credenciais do projeto Supabase.
# Para rodar localmente: copie este arquivo para app/.env (sem .example) e preencha.
# Para builds de produção EAS: use `eas secret:create --name EXPO_PUBLIC_SUPABASE_URL --value ...`
#
# Onde pegar: painel Supabase → Settings → API
#
# NUNCA commite o app/.env (já está no .gitignore).

EXPO_PUBLIC_SUPABASE_URL=https://XXXXXXXXXXXX.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...chave_anon_aqui
